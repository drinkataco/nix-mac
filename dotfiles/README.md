# Dotfiles

File-based dotfiles live here in their native formats.

Current convention:

- `.config/...` files go under `dotfiles/.config/...`
- top-level home files like `.zshrc` can later live directly under `dotfiles/`

Start with:

- `dotfiles/.config/alacritty/alacritty.toml`
