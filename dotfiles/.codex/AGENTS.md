# Global Codex Instructions

When working in a repository:

- Use the repository's `AGENTS.md` if it exists.
- If no `AGENTS.md` exists, use the repository's `CLAUDE.md` if it exists.
- If neither repository file exists, fall back to `~/.claude/CLAUDE.md` if it exists.
